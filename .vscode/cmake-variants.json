{
  "buildType": {
    "default": "Debug",
    "choices": {
      "Debug": {
        "short": "Debug",
        "long": "Debug build without tests",
        "buildType": "Debug",
        "settings": {
          "RUN_TESTS": "OFF"
        }
      },
      "Release": {
        "short": "Release",
        "long": "Release build without tests",
        "buildType": "Release",
        "settings": {
          "RUN_TESTS": "OFF"
        }
      },
      "Test": {
        "short": "Test",
        "long": "Build unit tests",
        "buildType": "Debug",
        "settings": {
          "RUN_TESTS": "ON"
        }
      }
    }
  }
}